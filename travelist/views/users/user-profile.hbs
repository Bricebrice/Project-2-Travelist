{{#if userInSession}}
  <h2>Welcome, {{userInSession.firstName}} {{userInSession.lastName}}!</h2>
  <p>This is your profile page {{userInSession.username}}!</p>
  <button>
    <a href="/userProfile">Reload the page</a>
  </button>
{{else}}
  <p>Still no logged in user, sorry!</p>
{{/if}}